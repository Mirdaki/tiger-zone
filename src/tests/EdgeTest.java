package tests;
//DEPRECATED
/*import static org.junit.Assert.*;

import org.junit.Test;

import entities.edge;

public class EdgeTest {

	@Test
	public void edgeConstructorAndGetterTest() {
		edge e1 = new edge('J', 'T', 'J');

		assertEquals('J', e1.getTop());
		assertEquals('T', e1.getMid());
		assertEquals('J', e1.getBot());
		assertEquals('T', e1.getType());
	}

	@Test
	public void edgeSetterTest() {
		edge e1 = new edge('J', 'J', 'J');
		e1.setTop('L');
		e1.setMid('L');
		e1.setBot('L');
		e1.setType('L');

		assertEquals('L', e1.getTop());
		assertEquals('L', e1.getMid());
		assertEquals('L', e1.getBot());
		assertEquals('L', e1.getType());
	}

	@Test
	public void edgeEqualsTest() {
		edge e1 = new edge('J', 'T', 'J');
		edge e2 = new edge('J', 'T', 'J');
		edge e3 = new edge('L', 'L', 'L');

		assertTrue(e1.equals(e2));
		assertFalse(e1.equals(e3));
	}

	@Test
	public void edgeToStringTest() {
		edge e1 = new edge('J', 'T', 'J');

		assertEquals("Top: J Mid: T Bot: J", e1.toString());
	}
}
*/
